[pytest]
addopts = -ra
testpaths = tests
markers =
    unit: unit tests (fast, deterministic)
    api: API tests (FastAPI TestClient)
    ui: UI tests (Playwright)
    perf: performance checks (pytest-benchmark)
    slow: slower or heavier tests
